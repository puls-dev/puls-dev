{
  "name": "puls-monorepo",
  "private": true,
  "description": "Monorepo for Puls intent-driven IaC",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @puls-dev/core && npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "ts-node": "^10.9.2",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@puls-dev/aws": "^0.5.3",
    "@puls-dev/azure": "^0.5.3",
    "@puls-dev/cloudflare": "^0.5.3",
    "@puls-dev/core": "^0.5.3",
    "@puls-dev/do": "^0.5.3",
    "@puls-dev/firebase": "^0.5.3",
    "@puls-dev/gcp": "^0.5.3",
    "@puls-dev/hcloud": "^0.5.3",
    "@puls-dev/proxmox": "^0.5.3"
  }
}
